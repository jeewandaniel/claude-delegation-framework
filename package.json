{
  "name": "claude-delegation-framework",
  "private": true,
  "description": "Global Claude Code framework: model delegation ladder, evidence rules, subagent ledger, manual /handoff",
  "scripts": {
    "test": "node --test tests/*.test.js"
  }
}
