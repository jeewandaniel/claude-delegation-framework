{
  "name": "claude-delegation-framework",
  "private": true,
  "description": "Global Claude Code framework: model delegation ladder, evidence rules, subagent ledger, manual /handoff, optional context-hygiene module",
  "scripts": {
    "test": "node --test tests/*.test.js modules/*/tests/*.test.js"
  }
}
